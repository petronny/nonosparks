apply plugin: 'java'

dependencies {
        compile fileTree(dir: 'libs', include: ['*.jar'])
}
sourceSets {
    main {
        resources {
            srcDirs = ['src/main/res']
            include '**/*.json'
            include '**/*.png'
        }
    }
}
jar {
        manifest {
                attributes 'Main-Class': 'main.java.nonosparks.SwingDemo'
        }
        from {
                configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
        }
        destinationDir = file('.')
}
